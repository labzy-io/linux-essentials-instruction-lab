# Step 10: Basic Scripting

1. Create file: `nano greet.sh`
2. Add:
```bash
#!/bin/bash
echo "Hello, $1!"
```
3. Save & exit (Ctrl+O, Enter, Ctrl+X)
4. Make executable: `chmod +x greet.sh`
5. Run: `./greet.sh Alice`

